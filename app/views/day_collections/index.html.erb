<!--
<h1>Listing day_collections</h1>

<table>
  <tr>
    <th>User</th>
    <th>Status</th>
    <th>Shift</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<%# @day_collections.each do |day_collection| %>
  <tr>
    <td><%#= day_collection.user_id %></td>
    <td><%#= day_collection.status_id %></td>
    <td><%#= day_collection.shift_id %></td>
    <td><%#= link_to 'Show', day_collection %></td>
    <td><%#= link_to 'Edit', edit_day_collection_path(day_collection) %></td>
    <td><%#= link_to 'Destroy', day_collection, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<%# end %>
</table>

<br />
-->
<%= link_to 'New Day collection', new_day_collection_path %>

<%#=
calendar({:year => Date.today.year, :month => Date.today.month}) do |d|
  #cell_text = "#{d.mday}" 
  cell_text = "" 
  cell_attrs = {:class => 'day'}
  #cell_text << link_to( d.strftime('%d.%m.%Y'), overviewday_path(d.year ,d.month, d.day )) << "<br />" 
  
  @day.each do |e|
  if e.date == d 
	
	
     e.day_collections.each do |c|
         
      	 
     	 cell_text << c.user.name 
         cell_text << "<br />" 
     	 #cell_text << c.shift.name 

      end	 
      #cell_text << link_to( e.date.strftime('%d.%m.%Y'), overviewday_path(e.date.year ,e.date.month, e.date.day )) << "<br />" 
      cell_attrs[:class] = 'specialDay' 
  end
end

  [cell_text, cell_attrs]
end
#%>



<%=  calendar  Day.all, {:type => "week"}  %>
<%#=  calendar  Day.all  %>



